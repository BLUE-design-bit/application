# EtherCAT 功能覆盖范围审定

状态：功能、使用路径、PLC覆盖和Excel结构已确认，新版Excel已生成。PLC为汇川小型、汇川中大型、欧姆龙三组；具体型号/版本、测试条件及实测记录待补。此文件是新版Excel的功能范围来源，原109项Excel保留为旧版。

目标：开发、测试工程师亲手跑过参考品牌驱动器的 EtherCAT 功能，记录支持范围、实际表现和限制。条目按功能或行为组织，不编写操作教程。品牌保持 CDHD、台达、繁易、Elmo、KEBA（可选）。

## 1. 控制模式及模式切换：已确认

用户决定：删除 M08（IP及厂商扩展模式），其余9项保留。保留原编号以便追溯，不用其他条目填补 M08。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| M01 | CSP：周期同步位置 | 周期位置目标跟随、连续运动、位置偏置／前馈能力（如支持） |
| M02 | CSV：周期同步速度 | 正反向速度控制、零速、速度跟随、速度偏置（如支持） |
| M03 | CST：周期同步转矩 | 正反向转矩控制、转矩跟随、转矩偏置及速度限制（如支持） |
| M04 | PP：轮廓位置 | 绝对／相对定位、运动中接收新目标、目标确认与到位反馈 |
| M05 | PV：轮廓速度 | 正反转、加减速、目标速度更新、速度达到反馈 |
| M06 | PT：轮廓转矩 | 转矩目标更新、转矩斜坡、限幅及反馈 |
| M07 | HM：驱动器内部回零 | 支持的回零方法、完成／失败反馈、回零后的模式与位置状态 |
| M09 | 模式切换 | 哪些模式能够互切；未使能、已使能、运动中的切换限制；切换时的目标突变或异常动作 |
| M10 | 模式请求与反馈 | 请求模式与实际运行模式的一致性；不支持的模式或不允许的切换如何响应 |

结果含义：分别记录支持、不支持或受限及实际表现；品牌勾选表示已完成实测，不等于功能达标。仅资料确认不记为已实测。

## 全局要求：同时覆盖用户使用路径

用户新增要求：用户使用驱动器 EtherCAT 功能的路径都要遍历，明确包括导入 XML、ST、梯形图和调试界面。功能覆盖与入口覆盖同时成立；条目仍是需要验证的能力和行为，不改写成逐步操作教程。

后续 Excel 按“品牌 + 驱动/PLC版本组合 + 功能ID + 使用路径”保存独立记录。一个品牌功能格仅在全部适用路径和分支完成后汇总勾选，不能用某一个入口的成功代替其他入口。实际平台不具备的入口单独记录不适用及依据，缺软件/硬件或尚未测试不能记为不适用。

路径细分草案如下，具体清单后续单独交用户审定：

| 路径类型 | 应记录的覆盖分支 |
| --- | --- |
| XML / ESI 与工程配置 | 正式XML导入、版本更新；离线建站、在线扫描；正式设备描述与在线描述回退（如有） |
| ST 程序 | 客户实际使用的运动功能块、参数访问功能块及程序逻辑 |
| 梯形图程序 | 对应功能的梯形图调用及程序逻辑，单独留证，不沿用ST的结果 |
| PLC 调试界面 | 轴调试、点动/定位、参数在线读写、状态与报警查看等实际可用入口 |
| 控制方式 | 厂商运动库/原生轴控制与直接读写PDO、手写CiA402控制分别记录；与编程语言组合覆盖 |
| 参数下发方式 | 启动参数自动写入、程序主动SDO访问、在线对象浏览器读写及其并存/覆盖行为 |
| 厂家调试工具 | 明确实际经过EtherCAT、EoE或独立USB/串口/其他接口；独立接口成功不能代替EtherCAT路径 |
| 客户应用触发 | 适用的HMI操作、自动程序触发、多个入口并存与控制权交接 |

XML导入属于配置路径，ST/梯形图属于程序表达入口，PLC调试界面属于操作入口；它们不是同一维度的互斥选项。例如“导入ESI + ST + 运动库”与“导入ESI + 梯形图 + 运动库”应能分别追溯。用户要求覆盖全部适用路径，不在未经确认的情况下以抽样代替。

## 2. PDO / SDO 与对象访问：已确认

用户决定：D01—D09全部保留。D07按设备是否提供需要分段传输的大对象确定适用性；D06、D08、D09按下表澄清后的范围保留。参考品牌不支持某能力也要记录依据，不能与未测试混同。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| D01 | 默认及预设PDO | 提供的组合及各组合包含的控制、反馈和诊断数据 |
| D02 | 自定义PDO映射 | 映射对象调整、数量/长度/组合和可修改状态的限制 |
| D03 | PDO数据交换 | 数据类型、正负值、位宽、更新表现及无效数据/错误映射响应 |
| D04 | SDO读写 | 对象、权限、可写状态及生效时机 |
| D05 | SDO错误响应 | 不存在对象、只读对象、越界值和错误长度的响应 |
| D06 | 同一对象多个子项的整组访问（Complete Access） | 对实际支持对象验证整组读取/写入及限制；不能与任意多个对象的批量访问混同 |
| D07 | 大对象完整传输及分段SDO | 对实际存在的大对象验证完整传输；确认是否确实超过单次邮箱传输容量，不能把读短字符串等同验证分段 |
| D08 | 运动中读写参数 | 运动同时读诊断/参数、修改允许在线调整的参数，以及频繁访问对运动/通信的影响 |
| D09 | PDO / SDO 数据含义与一致性 | 对两条路径均可访问的对象，明确单位/缩放/权限/更新时机及生效差异；允许有文档依据的换算，不强制原始数字相等 |

### D06—D09 场景解释与资料依据

- D06：把同一对象下的多个子项作为一组访问。例如主站初始化时整组下发配置，而不是逐子项访问。它描述“访问哪些子项”，并不保证数据都在一个网络帧里完成。[Beckhoff CoE说明](https://infosys.beckhoff.com/content/1033/tc3_io_intro/1357984011.html)、[CODESYS启动参数的Full access](https://content.helpme-codesys.com/en/CODESYS%20EtherCAT/_ecat_edt_module_start_parameter.html)。
- D07：某个对象的数据超过邮箱可承载长度时，分多段传完。工程场景举例是设备确实提供的较长字符串、大数组或厂商数据块；它们是否存在、是否足够大须查实际对象字典。不能用FoE固件文件传输代替SDO分段。[ETG.1500 §5.7.2及5.7.3](https://www.ethercat.org/download/documents/ETG1500_V1i0i2_D_R_MasterClasses.pdf)，本次获取官方搜索索引中的相关段落，PDF直接抓取返回403。
- D08：工程场景举例是生产运动继续运行时，由PLC/HMI读取诊断数据，或用在线工具调整明确允许在线修改的参数。需分别了解可读/可写范围与访问负载下的行为，不能把“未使能时SDO读写成功”当作运行中也可用。[Beckhoff CoE Online](https://infosys.beckhoff.com/content/1033/tc3_io_intro/1446522251.html)提供在线对象读写和自动更新入口；运动中负载测试为本项目工程建议。
- D09：PLC使用PDO反馈，同时调试界面通过SDO查看同一对象时，需要知道数字的含义是否一致、是否需换算。Kollmorgen AKD官方资料确实列出某些配置下PDO与SDO位置缩放不同。该例只证明此类差异存在，不代表五个参考品牌均如此。动态量应考虑采样时刻，不能直接比两个不同时刻的数值。[Kollmorgen AKD位置缩放](https://www.kollmorgen.com/en-us/developer-network/position-scaling-akd-drive-ethercat-communication)。

以上资料于本轮答疑核查；应用举例与验收建议不构成对参考品牌已支持功能的声明。

## 3. 使能、停止与复位：已确认

用户决定：S01—S09全部保留。对实际平台支持的ST、梯形图、PLC调试界面和直接对象控制等入口分别留证；厂家工具记录其实际通信路径。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| S01 | 驱动状态与反馈 | 控制字命令与状态字反馈、允许的状态转换、无效命令/组合的响应 |
| S02 | 使能与撤使能 | 静止及运动中的使能/撤使能表现、重复使能、实际转矩与状态反馈 |
| S03 | 使能前置条件 | 主电源、硬件使能、STO等实际具备条件对EtherCAT使能的影响；条件未满足时的反馈及恢复行为 |
| S04 | PLC停止与暂停 | MC_Stop、MC_Halt或平台等效功能的减速、轴状态、后续命令接受条件与驱动实际表现 |
| S05 | 驱动快速停止（Quick Stop） | 可选停止方式、减速参数、停止后是否保持使能/转矩以及退出快速停止的条件 |
| S06 | 驱动暂停（Halt） | 各支持模式下的暂停、保持、恢复及目标处理；与PLC暂停入口分别留证 |
| S07 | 故障复位 | 故障仍存在/已经消除时的复位响应，驱动故障与PLC轴错误能否分别清除 |
| S08 | 停机或复位后恢复运动 | 是否需要重新使能、旧目标/旧命令的处理、位置连续性以及是否意外续跑 |
| S09 | 控制入口交接与冲突 | PLC程序、PLC调试界面、厂家工具等并存时的控制权、冲突响应与交接表现 |

S04记录PLC功能块这一层的能力，S05/S06记录驱动控制这一层的能力，实测时明确实际映射关系，不按名字合并为同一测试。S03关注安全/硬件条件与EtherCAT控制的联动，不以此代替独立安全功能验证。

## 4. 单位、坐标与位置反馈：已确认

用户决定：U01—U10全部保留。记录功能由驱动器还是PLC实现；各操作入口的换算、反馈与实际机械运动应能对照。功能不存在与测试尚未完成分别记录。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| U01 | 位置单位与电子齿轮 | 指令/反馈计数与实际转角或位移的换算、电子齿轮/传动比设置及其范围 |
| U02 | 速度单位 | 速度指令、反馈、限值的单位与换算，PLC显示和驱动反馈的对应关系 |
| U03 | 转矩单位 | 转矩指令、反馈、限值采用的基准与比例，例如相对额定转矩的比例或实际转矩单位 |
| U04 | 方向与极性 | 正反方向定义、方向反转设置对指令/反馈以及回零/限位方向的影响 |
| U05 | 坐标设定与零点偏置 | 驱动设零/偏置、PLC设位置等实际入口的支持范围，对反馈/目标/机械运动及重启保持的影响 |
| U06 | 绝对位置与掉电保持 | 实际支持的单圈/多圈位置、PLC或驱动重启后的机器坐标、断电移动后的反馈与位置有效性 |
| U07 | 增量位置与参考有效性 | 上电位置初值、回零/参考有效标志、重启或反馈异常后需要重新建立参考的条件 |
| U08 | 旋转轴与模数坐标 | 跨零/跨圈反馈、圈数、正反向与最短路径等实际可用定位方式；驱动与PLC各自的实现和限制 |
| U09 | 位置计数边界 | 实际位宽、正负边界、溢出/回绕；支持连续回绕时的表现，禁止跨界时的限制或错误响应 |
| U10 | 位置反馈来源 | 电机编码器、负载编码器、第二路反馈等实际存在的来源，EtherCAT可读取哪些值、能否选择及各自单位 |

同一参数既可在PLC设置又可在驱动设置时，记录两端各自作用及组合后的换算，避免将重复缩放或PLC实现能力误记为驱动能力。U08/U10按实际硬件与平台能力确定适用范围，不要求所有驱动具备多圈、模数规划或双编码器。

## 5. 回零、限位与位置锁存：已确认

用户决定：H01—H11全部保留。M07保留回零模式的总体覆盖，本组细化回零及相关输入/锁存功能；关联同一份完整证据，不要求重复执行相同测试。各功能仍按全部适用入口留证。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| H01 | 驱动内部回零方法 | 实际提供的原点开关、限位、编码器Z相、当前位置等回零方法及各自适用条件 |
| H02 | PLC主导回零 | PLC运动库/回零功能块与该驱动的配合，输入接PLC或驱动时的可用路径及限制 |
| H03 | 回零参数 | 搜索方向、搜索/退出速度、加速度、原点偏置等实际可配置参数及生效表现 |
| H04 | 回零异常与中断 | 开始时已压住开关、找不到原点/索引、触及限位、暂停/撤使能/中断后再次回零的表现 |
| H05 | 回零结果与重复性 | 完成/失败反馈、参考有效性、重复回零的位置偏差以及回零后正常运动的坐标衔接 |
| H06 | 正负软件限位 | 设定范围、启用条件、越界目标/实际越界响应、退出限位方向以及改变零点后的作用 |
| H07 | 正负硬件限位 | 限位输入、停止/报警反应、允许脱离的方向、恢复条件，以及不同模式下的作用 |
| H08 | 原点与限位输入的配置和读取 | 输入源、极性、滤波等实际能力，EtherCAT可读取的输入状态及与真实信号的对应关系 |
| H09 | 位置锁存（Touch Probe） | 可用通道/触发源、上升沿/下降沿、单次/连续锁存、清除和重新触发能力 |
| H10 | 锁存反馈与连续事件 | 锁存位置来源、精度、时间戳（如有）、新数据标志以及快速连续触发时的覆盖/丢事件表现 |
| H11 | 利用锁存结果的PLC功能 | 中断定位、测长等实际可用应用，PLC功能块能否正确取得锁存数据并完成后续动作 |

H01/H02分别记录驱动内部和PLC主导的回零能力，不把同名回零功能块的实现视为相同。H11明确区分驱动提供的锁存数据与PLC实现的应用功能。没有时间戳、多通道或某回零方法时记录实际能力边界，不假定每台驱动都提供。

## 6. 同步、通信异常与恢复：已确认

用户决定：N01—N12全部保留。记录通信状态、驱动状态、PLC轴状态和机械行为之间的对应关系。本组将已确认S组的控制行为放入具体通信异常场景，完整证据可关联复用，不重复安排相同测试。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| N01 | EtherCAT通信状态 | INIT、PREOP、SAFEOP、OP的进入/退出条件，各状态下可用的参数/过程数据访问及输出行为 |
| N02 | 通信周期范围 | 实际支持的周期、最短/最长周期、模式与PDO配置对周期能力的限制，以及不支持周期的响应 |
| N03 | 同步方式 | DC分布式时钟、SM事件同步、FreeRun等实际支持方式，各方式的配置限制及运动表现 |
| N04 | 同步异常与时序容限 | 周期抖动、任务超时、同步失锁/配置不匹配时的检测、报警、运动反应与重新同步 |
| N05 | 通信看门狗 | 超时检测是否提供、配置范围、触发条件、检测与反应时间，以及超时后的驱动状态 |
| N06 | 断线与重连 | 持续/间歇断链、重新接通时的节点状态、运动反应、自动或手动恢复能力 |
| N07 | PLC停止与继续运行 | PLC STOP/PROGRAM/RUN等实际操作下，过程数据是否继续更新、驱动如何反应、恢复运行后如何处理 |
| N08 | PLC重启与工程变更 | PLC/运行时重启、工程下载或重新激活后的重新建站、同步、参数及轴状态恢复能力 |
| N09 | 驱动电源中断 | 控制电与主回路电分别中断/恢复时的通信、报警、实际停机及本轴/下游节点影响 |
| N10 | 上电顺序与延迟上线 | PLC先上电、驱动先上电、主电源晚到和从站延迟上线时的等待、超时及恢复表现 |
| N11 | 通信恢复后的运动接管 | 通信恢复后是否需清故障/重新使能，旧目标/旧命令的处理、位置连续性和意外续跑风险 |
| N12 | 通信继续但目标不再更新 | PLC轨迹计算停止而总线仍发送旧目标时，是否能识别、由哪层处理、允许保持什么及保持多久 |

N12不强制驱动实现某一种应用层检测机制，要求摸清主站/应用/驱动各方的实际能力和责任边界。N09及其他运动故障注入遵循已有受控台架要求。断线后PLC缓存的旧反馈不能作为实际机械已停止的证据，应使用仍有效的本地记录或外部测量。

## 7. 参数保存、启动配置与设备更换：已确认

用户决定：P01—P10全部保留。关注配置在启动、断电、迁移和维护后的实际结果；与D组对象读写、N组重启恢复的完整证据关联复用，不重复执行相同测试。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| P01 | EtherCAT可配置范围 | 电机/编码器、控制增益、滤波、制动器、I/O等实际参数中，哪些能从PLC经EtherCAT配置，哪些必须使用厂家工具及其通信接口 |
| P02 | 参数生效与联动 | 在线立即生效、撤使能后生效、重启后生效的区别；关联参数能否成组更新，中途失败时是否留下部分配置 |
| P03 | 参数保存与掉电保持 | 临时值与永久值的区别、保存入口、保存完成/失败反馈，以及重启和断电后的实际保持范围 |
| P04 | 启动参数与现有参数的覆盖 | PLC启动参数、驱动已保存参数、在线修改值和PDO周期写入并存时，实际采用哪一个值、何时被覆盖及如何追溯来源 |
| P05 | 启动参数下发异常 | 参数依赖/顺序、下发状态、超时、拒写或部分失败时的提示、建站结果及重试后的配置完整性 |
| P06 | 参数备份与恢复 | PLC或厂家工具可导出/恢复的范围、遗漏项、差异比较，以及恢复后的实际配置与原机是否一致；记录实际传输接口 |
| P07 | 恢复默认设置 | 支持整机/分组恢复哪些默认值，对通信、轴配置及重新接入的影响；厂家工具与EtherCAT入口分别记录 |
| P08 | 设备与版本匹配 | 型号、产品标识、硬件/固件版本与ESI、PLC工程的匹配检查；不匹配时能否识别、提示并定位差异 |
| P09 | 更换驱动与配置迁移 | 同型号同版本及厂家声明兼容的版本之间，更换后哪些配置可自动恢复、哪些需人工补齐；节点身份、轴对应和机器专用参数是否正确 |
| P10 | 固件升级与升级后兼容性 | 是否支持经EtherCAT升级（如FoE），实际限制、失败/中断恢复能力，以及升级后的参数、对象字典、PDO和原PLC工程兼容性 |

P01不要求所有整定功能都能通过EtherCAT完成，只要求明确交付时客户能走的真实路径。P08的版本匹配与后续XML/ESI入口覆盖关联。P09不默认跨型号或任意版本可迁移；不可复制的设备校准值及机器专用参数需明确边界。P10按实际升级能力确定适用性，独立USB等接口升级不能记为EtherCAT升级成功；仅提供FoE传输也不能证明支持固件升级。

## 8. 报警、诊断与现场排障：已确认

用户决定：A01—A09全部保留。关注客户通过PLC及其他实际入口能够获得的诊断信息和故障证据；与S组、N组已覆盖异常关联复用，分别记录故障本身的行为和诊断可见性。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| A01 | 当前故障与警告 | 可读取哪些故障/警告、严重程度及详细原因；仅预警、限制运行与停机故障如何区分，当前状态是否及时更新 |
| A02 | PLC与驱动报警对应 | PLC轴错误、EtherCAT通信错误、驱动故障码与厂家工具显示如何对应；能否从PLC获得原始原因而非只有通用错误 |
| A03 | 短时与连续报警事件 | 实际提供的事件通知/主动上报或轮询机制，短时出现后消失、连续发生及重复故障能否被PLC捕获；延迟、去重和丢失边界 |
| A04 | 故障历史与保持 | 历史容量、先后顺序、时间信息及故障现场值；清故障、重启、掉电后的保留情况及清除入口 |
| A05 | 多故障与最初原因 | 多个故障同时或先后发生时，首个原因、后续连锁故障和仍未消除的原因能否区分，显示/上报优先级如何处理 |
| A06 | 无报警但不动作的诊断 | 未使能、模式未就绪、限位、指令未接受、控制权冲突等场景，能否看到具体阻止原因及命令接受/执行状态 |
| A07 | 运行状态与诊断量 | 实际开放的跟随误差、负载/转矩、母线电压、温度、I/O及通信诊断量的访问入口、单位、刷新速度和有效性；区分主站统计与驱动提供的数据 |
| A08 | 波形与故障现场捕获 | PLC采样和驱动内部记录各能捕获哪些量、采样周期/长度、触发及故障前后数据；能否经EtherCAT取得，断线后哪些证据仍可保留 |
| A09 | 诊断资料导出与追溯 | 报警历史、波形、参数快照、通信状态及设备/软件版本能否导出并关联到同一故障；明确需从哪些入口分别取得及时间能否对齐 |

A03不预设设备支持某种主动上报机制，实际只有轮询时也需记录可捕获范围。A06各场景按实际报警行为归类，不假定所有驱动均不报警。A07/A08不要求所有监测量或内部记录功能必备，PLC周期采样与驱动内部高速采样分别记录，不能混作相同性能。厂家工具通过独立接口获得的信息不代表PLC可通过EtherCAT获得；故障时的旧缓存数据需标明有效性。A09可关联多份导出资料，不强制设备提供一键诊断包。

## 9. PLC接入与用户使用路径：已确认

用户决定：E01—E12全部保留。全局要求中的全部适用路径覆盖仍有效。本组描述各入口应跑通的能力与行为，不编写点击、编程或配置教程；功能测试结果按实际使用的路径组合关联。

| 编号 | 使用路径 | 覆盖范围 |
| --- | --- | --- |
| E01 | XML / ESI导入与更新 | 正式文件能否导入、正确显示型号/版本/PDO/同步等信息；旧新版本并存、替换和工程重新打开后的选择及解析结果 |
| E02 | 离线建站与在线扫描 | 离线选型建站、在线扫描识别、扫描结果与已有工程匹配；缺少正式ESI时平台实际提供的识别/回退能力及限制 |
| E03 | PLC轴创建与驱动绑定 | 平台原生驱动适配、通用CiA402轴、厂家模板等实际入口，轴与设备/PDO绑定、默认配置及反馈是否正确 |
| E04 | 运动库与厂家功能块 | PLC标准运动库、厂家专用库及示例工程实际可调用哪些运动/参数/诊断功能，命令接受、执行、完成、中断和错误反馈是否可用 |
| E05 | 直接PDO / 对象控制 | 不经运动库时，通过程序直接处理控制字、状态字、模式和目标/反馈，能否完成适用功能并处理异常；明确与轴库并存限制 |
| E06 | ST程序 | 经ST实际调用运动、参数访问与诊断功能；触发、连续更新、中断及复位的实际表现，不能沿用其他语言的测试结果 |
| E07 | 梯形图程序 | 经梯形图实际调用对应功能；触点保持、单次触发、重复触发及多功能块调用时的执行/反馈表现，独立留证 |
| E08 | 其他可用编程入口 | 平台实际提供的FBD/CFC、SFC等图形或顺序编程入口，适用功能的调用及动作衔接；与所调用的ST/梯形图等程序组合记录 |
| E09 | PLC调试与在线监视界面 | 轴调试、点动/定位、使能/复位、在线变量监视/写入等实际入口可完成哪些功能，显示是否正确及与程序控制的交接 |
| E10 | 参数访问入口 | 启动参数列表、ST/梯形图中的SDO功能块、在线对象浏览器等分别验证读写/反馈；入口并存和覆盖结果关联D组、P组 |
| E11 | 厂家调试软件接入 | 软件直连、经主站转接、EoE等实际可用路径的连接、配置、监视及与PLC并存能力；记录实际接口和主站依赖 |
| E12 | HMI与自动运行入口 | HMI按钮/配方、自动程序触发适用运动及参数功能，状态/错误能否传回，手动与自动切换及入口冲突如何处理 |

这些路径不是12选1：例如“正式ESI导入 + 通用CiA402轴 + PLC运动库 + ST”和相应梯形图组合分别留证。最终Excel按品牌、驱动/PLC/软件版本、功能ID和路径组合记录；本组用于维护入口覆盖矩阵，不将79项已确认功能机械地乘以12。对每项功能列出全部实际适用组合，缺设备或软件记为阻塞/未测试，入口不存在才有依据地记为不适用。平台/型号/版本的具体清单仍需单独确定，不能据此声称覆盖所有市售PLC。E04/E08/E12中的PLC应用能力与驱动本身能力分别归属；E11不预设每台驱动均支持EoE，独立接口成功不代替EtherCAT成功。

## 10. 运动指令衔接与多轴配合：已确认

用户决定：C01—C10全部保留。M组覆盖驱动运行模式，本组覆盖客户经PLC使用这些模式完成动作和多轴配合的能力。按实际平台/库和驱动组合分别记录，明确轨迹生成、同步关系、保护及错误处理各由哪一层实现；与已确认组关联完整证据，不重复执行相同测试。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| C01 | 点动与正反向切换 | 持续点动、松开停止、快速重复点动、运行中换向及正反命令同时出现时的动作、停止距离和状态反馈 |
| C02 | 运动中更新目标 | 定位或速度运动中修改目标位置、速度、加减速等实际允许的量，更新生效时机、轨迹连续性及不允许更新时的响应 |
| C03 | 连续指令、排队与打断 | 多段定位/运动的立即打断、排队执行、连续过渡等实际支持方式；命令顺序、交接速度、队列限制及被中断指令的反馈 |
| C04 | 运行倍率与动态限制 | PLC速度倍率、加速度/减速度倍率等实际能力及驱动限速/限矩共同作用时的运动表现；调低、恢复或超出允许范围时的响应 |
| C05 | 多轴同步运动 | 多轴共同起停、反向及连续轨迹时的实际位置/速度关系和同步误差；记录轴数、周期、负载和误差测量方法 |
| C06 | 电子齿轮跟随 | 虚拟/实际主轴等实际来源、比例/方向、接入/退出及主轴停转/反转时的跟随表现，运行中改比例和重新同步的限制 |
| C07 | 电子凸轮 | 实际支持的凸轮表、接入/退出、周期运行、相位/偏置调整和表切换能力，以及主轴停转/反转后的轨迹与同步表现 |
| C08 | 轴组与插补运动 | 平台实际支持的直线/圆弧等轴组运动、连续轨迹衔接、暂停/恢复及组状态反馈，记录实际轨迹误差和驱动配合限制 |
| C09 | 联动中的单轴异常 | 跟随轴/主轴故障、触限位、撤使能或掉线时其他轴及轴组的行为，解除联动、清错、重新接入和恢复后的相位/位置关系 |
| C10 | 一台EtherCAT设备控制多轴 | 单从站多轴产品的各通道对象/PDO/状态/报警是否对应，能否独立和同时运行，以及共享资源或单通道异常对其他通道的影响 |

C06的电子齿轮跟随关系与U01的单位/传动比换算分别记录。C05不能以同一扫描周期发出多条命令代替实际同步运动测量。C06—C08按平台实际能力测试，PLC生成同步目标不等于驱动内部具备齿轮、凸轮或插补功能，也不恢复已删除的M08厂商扩展模式范围。C10仅适用于单EtherCAT从站多轴产品，多台单轴驱动不能代替此项；没有相应产品时记录不适用及依据。

## 11. 网络组合、容量与长期运行：已确认

用户决定：T01—T09全部保留。关注单机功能在实际网络组合及持续运行条件下的表现；与N组通信恢复、C组多轴运动及E组接入路径关联证据。具体轴数、网络组合、测试时长/次数和量化判据在范围确认后统一确定，不以一组配置推断无限容量或全部组合兼容。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| T01 | 多台驱动识别与轴对应 | 同型号多台、不同型号/品牌驱动共网时的识别、轴绑定及数据/报警对应；节点顺序或位置变化后的匹配、拒绝和提示行为 |
| T02 | 混合从站与网络拓扑 | 驱动与I/O、耦合器等从站共网，串接及实际支持的分支拓扑下的建站、端口连接限制、同步和数据交换表现 |
| T03 | 参考时钟与节点排列 | 实际采用哪台设备作为DC参考时钟、驱动能否承担该角色；允许的参考时钟/节点排列变化后同步能否建立，运行误差有何变化 |
| T04 | 轴数、PDO与周期的组合容量 | 多轴及I/O在不同PDO长度、目标周期下能否同时运行，主站/网络负载、同步误差及越过实测可用范围时的表现 |
| T05 | 后台通信与运动并行 | HMI刷新、SDO读写、在线监视、波形获取及实际支持的EoE/文件传输等同时进行时，运动/通信受何影响，各类访问是否受限或超时 |
| T06 | 长时间连续运行 | 持续执行约定运动及正常后台访问后的周期/同步稳定性、运动误差、累计通信错误、偶发报警及性能变化 |
| T07 | 反复启停与恢复 | 反复使能、运动、停止、允许的重启/断线恢复后，是否出现偶发失败、状态残留、参数/位置变化或恢复时间增长 |
| T08 | 热连接与可选站组 | 实际支持时，预配置设备组在运行中缺席、加入、移除及再次加入的识别、同步、轴状态恢复，以及对其余节点的影响 |
| T09 | 电缆冗余 | 实际支持时，冗余链路断开、恢复或冗余能力耗尽后的通信、运动和诊断表现；记录所需主站、布线及从站限制 |

T03按平台允许方式选择或改变参考时钟，不假定每台驱动都有该能力。T04仅报告已验证组合，不将主站/库容量限制归为驱动固有限制。T05记录各服务允许运行的状态；要求退出运动状态的传输记录其限制，不强行并行，也不将FoE传输等同于固件升级。T06/T07分别记录测试条件、总时长/次数、失败次数及最差表现，不能仅写“稳定”。T08的热连接与N06普通断线重连分别记录；T09明确为电缆冗余，不暗指主站冗余。T08/T09按实际系统能力确定适用性，缺相应台架记为阻塞/未测试，确认不支持才记录能力边界。

## 12. 运动监控、限幅与辅助I/O：已确认

用户决定：R01—R08全部保留。补齐通过EtherCAT使用驱动监控、限制和辅助接口的能力；参数访问、单位、报警、限位及停机等已确认条目关联复用，不重复执行相同测试。

| 编号 | 功能 | 覆盖范围 |
| --- | --- | --- |
| R01 | 到位、速度达到与零速判断 | 实际支持的位置/速度窗口、持续时间、零速阈值及状态反馈；驱动达到标志、PLC功能块完成和实际机械状态如何对应，各模式含义是否不同 |
| R02 | 跟随误差监控 | 误差来源、窗口/超时参数、启用条件及支持模式，超限时的报警/停止与恢复；PLC监控和驱动监控分别设置或同时启用时的行为 |
| R03 | 速度限制 | 正负方向、不同模式下的限速范围和生效条件，超出限值的目标及实际超速如何处理，限幅/超速反馈能否经EtherCAT获得 |
| R04 | 转矩限制 | 正负限矩、运行中调整及多种限值同时存在时的实际作用，触及限制后的运动/状态反馈及退出表现；分别记录转矩模式与位置/速度模式 |
| R05 | 通用数字输入 | 除已覆盖的原点/限位/锁存外，驱动端输入能否作为PLC可读I/O，支持的功能分配、极性/滤波及响应表现；专用功能占用时的限制 |
| R06 | 通用数字输出 | PLC经EtherCAT控制驱动端输出的能力、输出与驱动内部功能的归属，启动/撤使能/故障/断线后的真实端子状态及保持规则 |
| R07 | 模拟量输入与输出 | 实际具备的通道能否经EtherCAT读取/设定，量程、单位、分辨率、刷新及饱和表现；内部功能占用或通信中断后的行为 |
| R08 | 抱闸控制与反馈 | EtherCAT使能/停机命令与自动抱闸时序的联动；实际支持的手动松闸/强制控制入口、允许条件、故障/断线时表现，以及反馈代表输出命令还是实测闸状态 |

R01不能将PLC逻辑完成或驱动状态位单独作为机械已到位的证据。R03区分目标限幅与实际超速监控；R04区分可写限值、实际采用限值和真实转矩，条件允许时关联独立测量。R05—R07按实际硬件和对象开放范围确定适用性，驱动有端子不代表PLC可自由访问。R08沿用已有受控台架及负载约束，仅覆盖控制联动，不代替抱闸或安全功能认证；没有实测反馈时应明确记录。通用输出与抱闸专用输出分别记录，避免将输出位改变当作机械抱闸动作成功。

## 首轮审定结果与下一阶段

已确认功能共106项：控制模式9项、PDO/SDO9项、使能/停止/复位9项、单位/坐标/反馈10项、回零/限位/锁存11项、同步/通信异常/恢复12项、参数保存/启动配置/设备更换10项、报警/诊断/排障9项、运动指令衔接/多轴配合10项、网络组合/容量/长期运行9项、运动监控/限幅/辅助I/O 8项。另已确认使用路径12项，路径条目与功能条目分开统计。M08保持删除。

本结果代表首轮范围审定完成，不代表品牌能力已查全、实测已完成或已通过验收。后续以具体型号手册、对象字典和现场问题核对覆盖缺口，新增范围仍单独交用户审定。

用户已确认PLC覆盖三组：汇川小型、汇川中大型、欧姆龙。分别登记平台组合、使用路径及实测结果，不合并为一个“汇川已测”或“PLC已测”。PLC型号、实际EtherCAT主站/运动模块、固件、工程软件及运动库版本在取得信息后逐项补齐；未知项保持待定，不替用户选型。现有TwinCAT、CODESYS研究保留为参考资料，不作为新增实测平台；欧姆龙具体系列亦不根据既有NJ/NX资料自行确定。

参考驱动品牌已确定为CDHD、台达、繁易、Elmo、KEBA（可选），具体型号、固件和ESI版本尚待登记。品牌、PLC平台、驱动型号及使用路径属于不同维度，Excel记录应能追溯至实际组合；平台信息未齐也可建立空白模板，但不能预填兼容性结论或已测勾选。

用户已批准三张工作表的结构，新版已按106项功能、12类路径及三组PLC生成；之后分阶段补齐具体设备、测试条件/判据和路径组合。原109项Excel保持不变，作为旧版留存。

## Excel结构：已确认并生成

用户决定：按三张工作表的方案先制作。新版文件为 [EtherCAT 功能与路径实测 v2](outputs/01a08f03-8e5c-79e3-bb07-33816e14789f/EtherCAT_功能与路径实测_v2.xlsx)，五个品牌保留在功能条目右侧，所有实测结果初始为空或未测试。

| 工作表 | 结构与用途 |
| --- | --- |
| 品牌勾选 | 106项功能 × 3组PLC，共318行；左侧为PLC组、功能域、编号、功能、覆盖范围，右侧依次为CDHD、台达、繁易、Elmo、KEBA（可选），可按PLC组/功能域筛选 |
| 路径与实测记录 | 按设备组合、功能编号、适用路径组合和覆盖分支登记待测与实测记录；填写测试人、岗位、日期、支持/受限/不支持及实际表现、证据和遗留问题 |
| 设备与测试条件 | 登记三组PLC与五品牌的具体设备组合、固件/ESI/软件/库版本、台架、周期/负载、判据，以及12类路径定义和适用性依据 |

品牌勾选项采用“未测试、部分实测、已实测、不适用、阻塞”的下拉状态，已实测显示勾选符号。仅在对应范围内的设备组合和全部适用路径/分支均已完成实测且证据齐全时记为已实测；不适用和阻塞不能算实测。支持与否在实测记录中单独描述。具体设备组合尚未登记、路径适用性尚未明确时，不允许将品牌格标为全部已实测。

318行表示覆盖计划，不等于已完成318项测试，也不将每行机械地乘以12类路径。路径记录按实际适用组合展开，同一品牌存在多个型号/版本时分别保留记录。相同功能描述保持一致，同一PLC组内多个设备组合的结果不得拼接成一个完整覆盖结论。

新版预留15个设备组合编号及1590条待展开路径记录，另有一条空白扩展行。组合编号不是具体设备或兼容性声明。每个组合/功能的路径清单由工程师确认列全，主表勾选由工程师核对全部适用记录后填写；自动“记录齐全”只检查必要字段，不判断证据真实性或完整功能覆盖。主表分别统计已勾选功能行数和实测记录齐全行数。

已验证源清单逐行一致、106项/12类路径/M08删除、318行/1590品牌格、15组占位、未填写证据、公式计算与记录扩展、日期和下拉、筛选与冻结窗格；已查看三张表的渲染并用独立只读程序复核导出文件。未使用真实设备执行测试，也未在原生Excel应用中验证全部交互。
